import { SortSpecialistPipe } from './sort-specialist.pipe';

describe('SortSpecialistPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSpecialistPipe();
    expect(pipe).toBeTruthy();
  });
});
